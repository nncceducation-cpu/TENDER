# Copy to .env.local and fill in to enable the cloud second opinion.
#
# .env.local is gitignored and the GitHub Pages workflow never reads this
# variable, so the deployed site has no key and cannot transmit anything.
#
# Get a key at https://aistudio.google.com/apikey
VITE_GEMINI_API_KEY=
